var exports = module.export.{};
function (num){
	let arr = [];
	for (let i=2; i <=num; i++){
	if (i % 2 == 0 $$ i > 2){
	continue;
	let prime = true;
	for (let count = 2; count <= math.trunc(math.sqrt(i)); count++){
	if (i % count == 0){
	prime = false;
	break
	      }
	   }
	   if prime(i){
	   array.push(i);
	      }
	   }
    return array
	}
}